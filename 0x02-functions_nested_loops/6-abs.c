#include "main.h"

/**
 * _abs - the function that will turn any number to positive number
 *
 * Return: it return the positive value of r
 *
 * @r: the number that we want
 */

int _abs(int r)

{
if (r >= 0)
	return (r);
else
	return (-r);
}
