#include <stdio.h>

/**
 * main - the main fucntion
 *
 * Return: always return the value 0
 *
 */

int main(void)
{
int i;
char T[] = "_putchar";
for (i = 0; i != '\0'; i++)
	putchar(T(i));

return (0);
}
