#include "main.h"

/**
 * main - the entry point
 *
 * Return: always return 0
 *
 */

int main(void)
{
int i;
char T[] = "_putchar";
for (i = 0; i != '\0'; i++)
	_putchar(T[i]);
_putchar('\n');

return (0);
}
