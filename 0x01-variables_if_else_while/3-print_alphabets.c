#include <stdio.h>

/**
 * main - the main function
 *
 * Return: it always return the value 0
 *
 */

int main(void)
{
char a;
char A;
for (a = 'a'; a <= 'z'; a++)
{
putchar(a);
}
for (A = 'A'; A <= 'Z'; A++)
{
putchar(A);
}

return (0);
}
