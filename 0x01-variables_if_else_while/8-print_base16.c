#include <stdio.h>

/**
 * main - the min function
 *
 * Return: always return the value 0
 */

int main(void)
{

int i;
int a;
for (i = 0; i <= 9; i++)
{
putchar('0' + i);
}
for (a = 'a'; a <= 'f'; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
