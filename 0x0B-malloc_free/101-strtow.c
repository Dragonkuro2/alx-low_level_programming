#include "main.h"
#include <string.h>

/**
 * strtow - this function is like the function strtow
 *
 * @str: the input pointer
 *
 * Return: it depands on the output of the function
 */

char **strtow(char *str)
{
if (str == NULL)
	return (NULL);
return (0);
}
