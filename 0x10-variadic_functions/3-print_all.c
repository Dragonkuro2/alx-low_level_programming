#include "variadic_functions.h"
#include <stdarg.h>

/**
 * print_all - this function prints all the type of arg
 * @format: the format of the arg
 * @...: the args
 */

void print_all(const char * const format, ...)
{
va_list list;
va_start(list, format);
return;
}
